# Benchmarks

Learning-focused benchmarks using `timeit`.

Run from repo root:

```bash
python -m project.dsa_pack.benchmarks.bench_queue_list_vs_deque
python -m project.dsa_pack.benchmarks.bench_sorting
```

Write your results into `project/dsa_pack/benchmarks/results/` as a short Markdown note (optional).
